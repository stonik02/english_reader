# This file is a template. Copy it to .env on the server; never commit .env.
PUBLIC_ORIGIN=https://krylov.reader.mooo.com

# Generate each value on the server, for example: openssl rand -hex 32
# Use letters and numbers only for POSTGRES_PASSWORD.
POSTGRES_PASSWORD=replace_with_a_long_random_alphanumeric_password
JWT_SECRET=replace_with_a_random_secret_of_at_least_32_characters
